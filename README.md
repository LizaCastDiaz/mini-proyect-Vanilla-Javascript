# mini-proyect-Vanilla-Javascript
I build a small application that does get data to an API and displays the data on screen. Then I deploy it in a totally free production so that anyone can access it.
